# Payments

`TestPaymentService` идемпотентно создаёт тестовую платёжную операцию и переводит
заказ в статус `processing`. Для будущего реального провайдера будет добавлен
отдельный адаптер после согласования его API. Секреты не сохраняются в payload.
