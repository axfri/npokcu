# Orders

`CheckoutService` повторно проверяет товар и вариант срока внутри транзакции,
фиксирует снимок позиции, выполняет тестовую оплату и передаёт оплаченный гостевой
заказ сервису привязки аккаунта.

Уникальный хеш токена формы обеспечивает идемпотентность повторного POST-запроса.
