# Mail

Письма об автоматическом аккаунте и привязке заказа ставятся в очередь после
commit. Очередь с учётными данными зашифрована; ошибки обрабатываются стандартными
failed jobs Laravel. Для писем с временным паролем используется отдельный
SMTP-транспорт без записи содержимого в лог.
